syntax = "proto3";

// Statistics reporting controls.
package lastlock.api;


// StatisticsLevel controls verbosity of device statistics.
enum StatisticsLevel
{
  // A good balance between battery life and data
  DEFAULT = 0;

  // Get a lot of data
  HIGH = 1;

  // Don't really care about data
  LOW = 2;
}
